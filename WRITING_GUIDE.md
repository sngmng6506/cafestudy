# Writing Guide

CafeStudy의 사용자 대상 문구 기준. 시각 스타일과 접근성 구현은 [DESIGN_GUIDE.md](./DESIGN_GUIDE.md)를 따른다.

## 목소리

문구는 다음 성격을 유지한다.

- **명확하게**: 무엇이 일어났고 무엇을 할 수 있는지 바로 이해된다.
- **간결하게**: 제목과 설명을 반복하지 않는다.
- **친근하게**: 사용자 대상 문장은 자연스러운 해요체를 사용한다.
- **존중하며**: 사용자를 탓하지 않고 해결 방법을 안내한다.

재미 기능은 장난스럽게 쓸 수 있지만 오류·삭제·개인정보 관련 문구는 명확성을 우선한다.

## 기본 원칙

### 결과가 드러나는 문구

버튼과 링크에는 실행 결과가 보이게 쓴다.

- `확인` → `모임 참여하기`
- `계속` → `사진 인증하기`
- `관리` → `대표 뱃지 바꾸기`
- `삭제` → `뱃지 삭제하기`

작은 팝업처럼 대상이 이미 명확한 경우에만 짧은 라벨을 허용한다.

### 한 문장에 한 가지 핵심

제목이 말한 내용을 본문에서 반복하지 않는다. 정보나 행동이 둘 이상이면 문장을 나눈다.

### 사용자 언어 사용

서버·DB·API 같은 구현 용어를 사용자에게 노출하지 않는다.

- `세션 토큰이 만료되었습니다.` → `로그인이 만료됐어요. 다시 로그인해 주세요.`
- `외부 크롤링 일정` → `소모임에서 가져온 일정`

### 같은 개념에는 같은 단어

화면마다 표현을 바꾸지 않는다. 다른 단어는 실제 행동이나 데이터 출처가 다를 때만 사용한다.

### 오류보다 복구 방법 우선

문제만 알리지 말고 다음 행동을 안내한다.

- `잘못된 사진입니다.` → `사진을 확인하기 어려워요. 밝은 곳에서 다시 찍어 주세요.`

성취 순간에는 보상이나 결과를 함께 알려도 되지만 과도한 감탄사는 반복하지 않는다.

## 문체

- 사용자 문장: 해요체
- 제목·탭·상태 라벨: 짧은 명사형 허용
- 관리자·개발 로그: 적용 대상 아님
- 느낌표: 특별한 성취 순간에만 사용
- 짧은 버튼·라벨에는 마침표를 쓰지 않음
- 반말과 과도한 유행어를 사용하지 않음

## 용어 사전

| 개념 | 기본 용어 | 구분 |
|---|---|---|
| 새 내부 모임 | 모임 만들기 | `모임 개설`과 혼용하지 않음 |
| 내부 모임 참석 | 참여하기 | 외부 서비스 `신청하기`와 구분 |
| 참여 철회 | 참여 취소하기 | `신청 취소`와 구분 |
| 외부 소모임 이동 | 소모임에서 신청하기 | 외부 서비스임을 표시 |
| 출석 증명 | 출석 인증 | 촬영 행동은 `사진 인증하기` 가능 |
| 앞으로 열릴 일정 | 예정 모임 | `열린 모임`은 모집 상태에만 사용 |
| 완료된 일정 | 지난 모임 | 기존 `모임 이력`은 점진적으로 통일 |
| 대표 이미지형 보상 | 대표 뱃지 | 적용/설정/변경을 행동에 맞게 사용 |
| 누적 보상 단위 | 포인트 | 점수와 혼용하지 않음 |
| 로그인 없이 보기 | 둘러보기 | `게스트`, `비회원` 같은 말을 화면에 쓰지 않음 |
| 로그인이 필요한 기능 | 로그인하면 쓸 수 있어요 | `권한 없음`, `이용 불가`로 쓰지 않음 |

여러 화면에 등장하는 새 용어는 이 표에 추가한다.

## UI 요소별 작성법

### 제목

- 내용 범위를 바로 알 수 있는 짧은 명사형
- `안내`, `정보`, `관리`처럼 모호한 제목은 구체화
- 제목과 첫 문장의 반복 금지

### 버튼과 링크

- 동사형으로 작성하고 실제 결과와 일치
- 위험 행동은 대상 포함: `모임 취소하기`, `뱃지 삭제하기`
- 외부 이동에는 서비스 이름 포함
- 아이콘 버튼의 `aria-label`도 같은 동작명 사용

### 빈 상태

현재 상태와 다음 행동을 함께 안내한다.

```text
예정된 모임이 없어요.
새 모임을 만들어 보세요.
```

`데이터가 없습니다.`만 단독으로 쓰지 않는다.

### 오류

1. 사용자 관점에서 무슨 일이 생겼는지
2. 지금 할 수 있는 해결 방법

- 내부 오류 코드와 HTTP 상태를 노출하지 않음
- 해결할 수 없는 문제는 재시도 또는 문의 방법 안내
- 입력 문제여도 사용자를 비난하지 않음

### 성공 메시지와 토스트

- 완료된 행동을 과거형으로 명확히 알림
- 포인트·랭킹·뱃지 같은 추가 결과가 있으면 함께 표시
- 화면에 결과가 이미 명확하면 중복 토스트 생략

### 확인 다이얼로그

- 제목에 위험 행동과 대상 표시
- 본문에 되돌릴 수 있는지와 영향 설명
- 버튼은 `돌아가기`와 실제 행동으로 구성

```text
이 모임을 취소할까요?
참여자에게서 일정이 사라지고 되돌릴 수 없어요.
[돌아가기] [모임 취소하기]
```

## 줄여야 할 표현

- `정상적으로`, `성공적으로`: 대개 불필요
- `해당`, `관련`, `진행`: 대상을 직접 쓸 수 있으면 제거
- `유효하지 않은`, `실패했습니다`: 구체적인 문제로 대체
- `확인`, `계속`, `완료`: 결과가 모호한 버튼에는 사용 금지
- API, 토큰, DB, 크롤링, payload 같은 개발 용어

## 문구 변경 체크

- 문구만 보고 다음 행동이나 결과를 예상할 수 있는가
- 같은 개념에 기존 용어를 사용했는가
- 해요체와 명사형이 역할에 맞는가
- 제목과 설명이 반복되지 않는가
- 빈 상태와 오류가 다음 행동을 안내하는가
- 사용자를 탓하거나 불필요하게 불안하게 하지 않는가
- 외부 이동과 위험 행동이 명확한가
